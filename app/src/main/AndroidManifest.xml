<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.digitalgeenie.comicbookapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PhoneAuthActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ComicsActivity" />
        <activity android:name=".NewArrivalsActivity" />
        <activity android:name=".TriviaActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".StoreLocatorActivity" />
        <activity android:name=".TriviaDetailsActivity" />
    </application>

</manifest>